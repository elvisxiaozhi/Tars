# Step {N} — {标题}

## 概要

一句话说明本步做了什么。

## 关键命令

```bash
# 编译 / 运行 / 测试等关键命令
```

## 文件清单

| 操作 | 文件 |
|------|------|
| 新增 | `path/to/file` |
| 修改 | `path/to/file` |
| 删除 | `path/to/file` |

## 设计决策

- 选了 A 而不是 B，因为 ...

## 红线遵守

- [ ] 没有引入硬编码密钥
- [ ] 没有跳过错误处理
- [ ] 编译通过，无 warning

## 验收结果

```
编译/运行输出贴这里
```

## 遗留问题

无 / 列出待后续处理的事项。
