# Step 2.11 — 三档止盈 + 移动止盈（Trailing Stop）

## 概要

基于隔夜 dry-run 数据分析（多笔交易触发 -50% 止损，但 MFE 显示大量浮盈曾回吐），实施三项优化：

1. 新增 TP0 中间止盈档位（45¢），将"大涨后全亏"转为部分获利
2. 移动止盈（Trailing Stop），基于 MFE 动态提升止损线
3. TP1 从 80¢ 降至 70¢，提高触发率

## 改动详情

### 1. 三档止盈结构

| 档位 | 旧 | 新 | 说明 |
|------|-----|-----|------|
| TP0 | — | 45¢ 卖 30%（3 shares） | **新增**，锁住中间浮盈 |
| TP1 | 80¢ 卖 50% | 70¢ 卖 30%（3 shares） | 降低触发门槛 |
| TP2 | 90¢ 卖 100% | 90¢ 卖剩余全部（4 shares） | 不变 |

sell_pct 计算：
- TP0: 0.30（30% of 100%）
- TP1: 3/7 ≈ 0.4286（30% of remaining 70% = 3 shares）
- TP2: 1.00（remaining 40% = 4 shares）

### 2. 移动止盈（Trailing Stop）

| 条件 | 止损线 | 说明 |
|------|--------|------|
| MFE ≥ 入场价 +25¢ | 入场价 +10¢ | 锁定至少 +$0.10/share 利润 |
| MFE ≥ 入场价 +15¢ | 入场价（保本） | 最差不亏 |
| MFE < +15¢ | 原 -50% 止损 | 不变 |

优先级：价格止损(-50%) > 移动止盈 > 最后10分钟 > 正常持有

trailing_stop 触发后也会 `set_candle_stopped()`，本蜡烛不再开仓。

### 3. 典型场景改善（示意）

| 场景 | 入场 | MFE 峰值 | 原结果 | 新逻辑预期 |
|------|------|----------|--------|------------|
| 高浮盈回吐 | 0.25 | 0.55 | 跌至 -50% 止损 | TP0 在 0.45 锁 30%，trailing 保本卖剩余 |
| 中浮盈回吐 | 0.25 | 0.43 | 跌至 -50% 止损 | MFE > +15¢ → 保本止损 |
| 短暂冲高后跳水 | 0.29 | 0.46 | 跌至 -50% 止损 | MFE > +15¢ → 保本止损 |

核心改进：将"大涨后全亏"的交易转为小赚/打平。

## 文件清单

| 操作 | 文件 | 改动 |
|------|------|------|
| 修改 | `src/core/strategy.cpp` | `compute_tp_levels()` 三档止盈 + `evaluate_exit()` 移动止盈 |
| 修改 | `src/core/strategy.h` | `close_reason` 注释更新 |
| 修改 | `src/main.cpp` | `trailing_stop` 加入 candle_stopped 逻辑 |

## 验收结果

```
cmake --build build   # 编译通过，零 warning
```
